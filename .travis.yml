language: node_js
node_js:
  - "12"
before_script:
  - npm install
script: npm run build
before_deploy:
  - cd $TRAVIS_BUILD_DIR
deploy:
  provider: pages
  cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master
notifications:
  email: false
